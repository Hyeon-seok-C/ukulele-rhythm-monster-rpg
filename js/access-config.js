/**
 * Remote access PIN — change before private deploy.
 * Client-side gate only; combine with private hosting (see PRIVATE-HOSTING.md).
 */
window.ACCESS_GATE_CONFIG = {
  pin: 'voxhyeon',
  storageKey: 'ukulele-rhythm-monster-auth',
};
